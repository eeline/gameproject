package characters;

public class Character extends Attributes {

	public Character(int maxHealth, int maxPower, int maxSpeed) {
		super(maxHealth, maxPower, maxSpeed);
		// TODO Auto-generated constructor stub
	}

}
